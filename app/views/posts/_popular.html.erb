<div class="row pop">
  <hr>
  <h1>MOST POPULAR POSTS</h1>
</div>

<div class="row">

  <% Post.except(@post).limit(4).each do |post|%>
    <div class="col-xs-6 col-sm-3 pop">
      <%= link_to image_tag(post.photo), post %>
      <h3><%= post.title %></h3>
    </div>
  <% end %>

</div>
